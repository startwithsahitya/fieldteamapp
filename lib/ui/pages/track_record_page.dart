import 'package:flutter/material.dart';

class TrackRecordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Track Record")),
      body: Center(child: Text("Track record / analytics will be shown here")),
    );
  }
}
